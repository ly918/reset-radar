# 贡献指南

欢迎通过 Issue 描述问题或提出改进，也欢迎提交 Pull Request。

提交前请运行 `./scripts/check.sh` 和 `./scripts/build-demo.sh`。涉及 UI 时检查深浅色、窗口尺寸和降低透明度；涉及真实模型调用时记录脱敏结果，不提交 Key、原始请求日志或个人本机路径。

- 把真实帖子、社区历史和合成测试数据分开。
- 模型分类、分类置信度、事件发生概率与已核验事件是不同概念。
- 新增第三方来源时保留出处、时间含义和使用限制。
- 安装包通过 Releases 分发，不把 `build/`、`dist/` 放入 Git。
- 贡献沿用本项目 MIT 许可；不要提交无权授权的第三方内容。
